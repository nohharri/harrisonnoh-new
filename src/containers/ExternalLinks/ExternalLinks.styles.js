import { colors } from '../../constants';

const styles = {
    container: {
        display: 'flex',
        fontSize: 40,
        color: colors.pink,
        width: '50%',
        margin: 'auto',
    },
    icon: {
        flex: 1,
        color: colors.pink,
    },
}

export default styles;