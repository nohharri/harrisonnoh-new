import { colors } from '../../constants';

const styles = {
    container: {
        display: 'flex',
        fontSize: 40,
        color: colors.pink,
    },
    icon: {
        flex: 1,
        padding: 50,
    },
}

export default styles;